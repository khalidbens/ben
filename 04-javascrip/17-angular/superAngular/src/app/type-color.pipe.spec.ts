import { PipeColorPipe } from './type-color.pipe';

describe('PipeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
